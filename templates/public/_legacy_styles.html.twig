<style>
    body {
        margin: 0;
        background: #dedede;
        color: #1f3550;
        font-family: Arial, Helvetica, sans-serif;
    }

    #container {
        min-width: 300px;
        margin: 0 auto;
    }

    #header {
        background: #2e2d2e;
        box-sizing: border-box;
        position: relative;
        left: 50%;
        transform: translateX(-50%);
        width: 100vw;
    }

    #logo {
        color: #666666;
        margin-top: 5px;
        font-size: 48px;
        text-align: center;
    }

    #logo a {
        color: #666666;
        text-decoration: none;
        font-size: 2rem;
    }

    #wrapper {
        max-width: 1200px;
        margin: 0 auto;
        padding: 20px;
    }

    #mainContent {
        min-height: 500px;
    }

    #footer {
        padding: 20px;
        border-top: 1px solid #ddd;
    }

    .inline-field {
        display: flex;
        align-items: center;
        gap: 12px;
    }

    .inline-field .legacy-label {
        margin: 0;
        white-space: nowrap;
        min-width: 80px; /* adjust as needed */
    }

    .inline-field .legacy-input {
        flex: 1;
    }

    .checkbox-inline {
        display: flex;
        align-items: center;
        gap: 8px;
    }

    .checkbox-inline .legacy-label {
        margin: 0;
    }

    .checkbox-inline input[type="checkbox"] {
        margin: 0;
        flex-shrink: 0;
    }

    .checkbox-field label {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        margin-right: 15px;
        margin-bottom: 0;
    }

    .checkbox-field input[type="checkbox"] {
        margin: 0;
    }

    .legacy-topbar {
        background: linear-gradient(90deg, #2d2d2d 0%, #222831 55%, #2e2e2e 100%);
        border-bottom: 1px solid #c0c0c0;
        box-shadow: inset 0 -1px 0 rgba(255, 255, 255, 0.15);
        margin: 10px 0 0;
        min-height: 68px;
    }

    .legacy-topbar-inner {
        box-sizing: border-box;
        color: #696969;
        font-size: 33px;
        font-weight: 700;
        line-height: 1;
        margin: 0 auto;
        max-width: 930px;
        padding: 6px 18px;
    }

    .legacy-page-shell {
        padding: 40px 16px 60px;
    }

    .legacy-card {
        background: #d3d3d3;
        border: 1px solid #777;
        border-radius: 6px;
        box-shadow: 0 2px 9px rgba(0, 0, 0, 0.45);
        margin: 0 auto;
        max-width: 900px;
        padding: 52px 30px 36px;
    }

    .legacy-form {
        margin: 0;
    }

    .legacy-html-text {
        color: #1f3550;
        font-size: 13px;
        line-height: 1.5;
        margin-bottom: 18px;
    }

    .legacy-required-note {
        color: #ff1313;
        font-size: 12px;
        font-weight: 400;
        margin: 10px 0 14px;
        text-transform: uppercase;
    }

    .legacy-field-group {
        margin-bottom: 18px;
    }

    .legacy-label {
        color: #ff1313;
        display: block;
        font-size: 13px;
        font-weight: 400;
        margin-bottom: 6px;
    }

    .legacy-input {
        background: #e8e8e8;
        border: 2px solid #c2c2c2;
        box-sizing: border-box;
        color: #1f3550;
        font-size: 13px;
        max-width: none;
        padding: 6px 8px;
        width: 100%;
    }

    .legacy-field-frame {
        background: #c6c6c6;
        border: 10px solid #bcbcbc;
        margin: 14px 0 12px;
        padding: 10px;
    }

    .legacy-field-row {
        align-items: center;
        display: grid;
        gap: 20px;
        grid-template-columns: 220px minmax(0, 1fr);
        margin-bottom: 12px;
    }

    .legacy-field-row .legacy-label {
        margin: 0;
    }

    .legacy-option-label,
    .legacy-list-option,
    .legacy-check-label {
        color: #3a4f66;
        font-size: 13px;
    }

    .legacy-lists {
        border: 0;
        margin: 16px 0;
        padding: 0;
    }

    .legacy-lists legend {
        color: #555;
        font-size: 14px;
        font-weight: 700;
        margin-bottom: 8px;
        padding: 0;
    }

    .legacy-list-options {
        display: flex;
        flex-direction: column;
        gap: 8px;
    }

    .legacy-list-option {
        align-items: flex-start;
        display: flex;
        gap: 8px;
    }

    .legacy-actions {
        align-items: center;
        display: flex;
        gap: 14px;
        margin-top: 10px;
    }

    .legacy-button {
        background: #dfdfdf;
        border: 1px solid #999;
        border-radius: 5px;
        color: #1f3550;
        cursor: pointer;
        font-size: 13px;
        line-height: 1;
        padding: 6px 10px;
    }

    .adminmessage {
        background: #e5e3b9;
        border: 1px solid #e1c873;
        color: #9d5f1a;
        font-size: 12px;
        margin-bottom: 14px;
        padding: 14px 14px 12px;
    }

    .adminmessage p {
        margin: 0 0 8px;
    }

    .adminmessage p:last-child {
        margin-bottom: 0;
    }

    .adminmessage .button {
        background: #ece4ba;
        border: 1px solid #9a9864;
        border-radius: 6px;
        color: #534d2d;
        display: inline-block;
        padding: 4px 8px;
        text-decoration: none;
    }

    .legacy-confirmation {
        color: #1f3550;
        font-size: 13px;
        margin: 10px 0 16px;
    }

    .legacy-powered-by {
        margin-top: 26px;
        text-align: center;
    }

    .legacy-powered-by img {
        display: inline-block;
    }

    .legacy-error-box {
        background: #fbe9e9;
        border: 1px solid #c93e3e;
        color: #8c1f1f;
        font-size: 13px;
        margin-bottom: 12px;
        padding: 10px;
    }

    .legacy-error-box p {
        margin: 0 0 6px;
    }

    .legacy-error-box p:last-child {
        margin-bottom: 0;
    }

    .legacy-success {
        background: #ebf7ea;
        border: 1px solid #6ca56a;
        color: #215f20;
        font-size: 13px;
        margin-bottom: 12px;
        padding: 10px;
    }

    .legacy-hidden {
        display: none;
    }

    @media (max-width: 720px) {
        .legacy-topbar-inner {
            font-size: 34px;
        }

        .legacy-card {
            padding: 28px 16px;
        }

        .legacy-field-row {
            gap: 6px;
            grid-template-columns: 1fr;
        }
    }
</style>
